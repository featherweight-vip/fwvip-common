// Example HDL top: instance the signal-level DUT + the VIP transactor modules and
// wire them on the bus interface(s). A `bind seats the transactor virtual
// interfaces the HVL-side agents drive. Ordinary module -- one file, not split.
module ip_hdl_top;
    // logic clk, rst;
    // dut u_dut(.clk(clk), .rst(rst), /* bus pins */);
    // <proto>_<role>_xtor u_agent(.clk(clk), .rst(rst), /* bus pins */);
endmodule
